先将字符串转换成数字和符号
构造数字栈和符号栈
如果是数字：压入数字栈
如果是符号：
    '(':        压入符号栈
    '+','-':    如果栈顶元素是null，或'(',直接压入
                如果栈顶元素是'-'，'+'，弹出栈顶元素和数字栈栈顶2元素，进行计算后压入数字栈
    ')':        如果栈顶元素是'(',直接弹出
                如果栈顶元素是'-'，'+'，弹出栈顶元素和数字栈栈顶2元素，进行计算后压入数字栈
