问题：判断String中是否存在1个以上的A，或连续两个以上的L
解决方案：
定义两个计数器，A的个数，连续L的个数
将String 转换成char数组
循环
    如果 char == A
    A计数器+1
        判断A计数器是否大于1

    如果 char == L
        L计数器+1
        判断L计数器是否大于2
    否则
        L计数器清零